#!/usr/bin/env ruby

begin
    require 'rpasswd'
rescue LoadError 
    path = File.expand_path(File.join(File.dirname(__FILE__),"..","lib"))
    raise if $:.include?(path)
    $: << path
    retry
end

Rpasswd::Digest.new.run(ARGV)
